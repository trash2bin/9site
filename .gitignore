# зависимости (нужны только для линтеров, сайт их не использует)
node_modules/

# артефакты сборки и рантайма
/site-server
/build/
*.log
*.pid

# отладочные скриншоты в корне (на /assets не влияет)
/*.png
/*.jpg
/*.jpeg

# система
.DS_Store

# локальная настройка Pi-агента, к проекту не относится
.pi/
.playwright-mcp/
